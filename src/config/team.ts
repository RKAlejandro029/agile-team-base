// Team management config.
//
// DEFAULT_NEW_USER_PASSWORD is pre-filled into the "Add user" dialog's temporary
// password field. Change this value to change the default for every user created
// from then on. It's just a starting point — the admin can still edit it per-user
// before creating the account, and every new user should change it after their
// first sign-in (see the "Change password" option in the sidebar).
export const DEFAULT_NEW_USER_PASSWORD = "Fintreas2026!";
